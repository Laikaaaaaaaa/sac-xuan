# OpenAI (poem generation)
OPENAI_API_KEY=
OPENAI_TEXT_MODEL=gpt-4o-mini

# Image provider: set to stability (VN->EN via OpenAI) / openai / gemini
IMAGE_PROVIDER=stability

# OpenAI (used for poem generation and for translating Stability prompts)
# Optional: override translation model (defaults to OPENAI_TEXT_MODEL)
OPENAI_TRANSLATE_MODEL=gpt-4o-mini

# OpenAI (direct image generation) - optional
OPENAI_IMAGE_MODEL=gpt-image-1
OPENAI_IMAGE_SIZE=1024x1024

# Stability AI (image generation)
STABILITY_API_KEY=
# Optional (used by Stability v1 fallback)
# STABILITY_ENGINE=stable-diffusion-xl-1024-v1-0
# Optional tuning
# STABILITY_STEPS=30
# STABILITY_CFG_SCALE=7
# Optional: force aspect ratio (otherwise derived from OPENAI_IMAGE_SIZE)
# STABILITY_ASPECT_RATIO=1:1

# Gemini / Google AI Studio (image generation) - optional
# Use either GEMINI_API_KEY or GOOGLE_API_KEY
GEMINI_API_KEY=
# GOOGLE_API_KEY=

# Imagen model name (default used by app if empty)
GEMINI_IMAGE_MODEL=imagen-3.0-generate-001

# Optional: force aspect ratio (otherwise derived from OPENAI_IMAGE_SIZE)
# GEMINI_IMAGE_ASPECT_RATIO=1:1

# (OPENAI_IMAGE_SIZE also used to derive aspect ratio for other providers)

# =============================================
# Music (Kie.ai Suno API / other Suno providers)
# =============================================
# Kie.ai base URL:
#   https://api.kie.ai
# Headers required:
#   Authorization: Bearer <YOUR_API_KEY>
#
# Required for Kie.ai Suno API:
#   - model
#   - callBackUrl (even if you plan to poll)
#
# Provider base + auth
SUNO_BASE_URL=
SUNO_API_KEY=

# Generate + status endpoints
SUNO_GENERATE_PATH=/api/v1/generate
SUNO_STATUS_PATH=/api/v1/generate/record-info?taskId={id}

# Kie.ai-required fields (auto-applied when SUNO_BASE_URL contains kie.ai)
SUNO_MODEL=V4_5PLUS
SUNO_CUSTOM_MODE=false
SUNO_INSTRUMENTAL=true

# Callback URL (must be public for real callbacks; for polling-only you can still set it)
SUNO_CALLBACK_URL=https://your-public-domain.com/api/suno-callback

# Optional: extra JSON merged into the request body (provider-specific)
# Example: SUNO_EXTRA_JSON={"title":"Tet Song","style":"Pop","negativeTags":"Heavy Metal"}
SUNO_EXTRA_JSON=
